import JoinForm from "./JoinForm";
import SectionWrapper from "../../ui/SectionWrapper";

const Join = () => {
  return (
    <SectionWrapper id="join">
      <JoinForm />
    </SectionWrapper>
  );
};

export default Join;